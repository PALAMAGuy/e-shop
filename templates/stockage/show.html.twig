{% extends 'base.html.twig' %}

{% block title %}Stockage{% endblock %}

{% block body %}
    <h1>Stockage</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ stockage.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ stockage.name }}</td>
            </tr>
            <tr>
                <th>Adresse</th>
                <td>{{ stockage.adresse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stockage_index') }}">back to list</a>

    <a href="{{ path('app_stockage_edit', {'id': stockage.id}) }}">edit</a>

    {{ include('stockage/_delete_form.html.twig') }}
{% endblock %}
